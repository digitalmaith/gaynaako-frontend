export interface Secteur {
  id: string;
  nom: string;
}

export interface Pays {
  id: string;
  nom: string;
  code?: string;
}

export interface DomaineIntervention {
  id: string;
  nom: string;
}